/** @file TSConfig for running scripts. See package.json. */
{
  "extends": "./tsconfig.base.esbuild.json",
  "compilerOptions": {
    "noEmit": true,
    "paths": {
      "@/": [
        "./scripts/*"
      ],
    }
  },
  "include": [
    "scripts"
  ],
}